{% extends 'base.html.twig' %}

{% block title %}Visualisation de pointures{% endblock %}

{% block body %}
    <nav>
        <a href="{{path('product_index')}}">Produits</a>
        <a href="{{path('brand_index')}}">Marques</a>
        <a href="{{path('category_index')}}">Categories</a>
        <a href="{{path('color_index')}}">Couleurs</a>
        <a href="{{path('size_index')}}">Pointures</a>
        <a href="{{path('stock_index')}}">Stocks</a>
    </nav>
    <h1>Pointures</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ size.id }}</td>
            </tr>
            <tr>
                <th>Pointure</th>
                <td>{{ size.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('size_index') }}">Retour à la liste</a>

    <a href="{{ path('size_edit', {'id': size.id}) }}">Modifier</a>

    {{ include('size/_delete_form.html.twig') }}
{% endblock %}
